# Agent Configuration
AGENT_NAME=BNB Chain AI Agent
AGENT_DESCRIPTION=Autonomous AI agent with on-chain identity
CHAIN=bsc-testnet

# Ethereum private key (0x-prefixed). Required for on-chain registration.
# Without this, agent runs in dev mode (no on-chain identity, no x402 payments).
PRIVATE_KEY=

# Server
PORT=3000
BASE_URL=http://localhost:3000

# Optional: Override default RPC endpoints
# BSC_RPC_URL=https://bsc-dataseed.binance.org
# BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545
